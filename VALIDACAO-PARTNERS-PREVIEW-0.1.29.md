# Validação — Partners Preview 0.1.29

- `app.js` validado com `node --check`.
- Confirmada remoção de **Novo bloqueio** do cabeçalho da Agenda.
- Confirmada ordem dos comandos renderizados: Abrir → Editar → Bloquear/Desbloquear → Excluir.
- Confirmada regra CSS final com duas colunas aplicada fora do breakpoint mobile, corrigindo também a visualização em desktop/tablet.
- Mantido o bloqueio por horário já existente na grade.
- Sem alterações de banco de dados.
